{
  "name": "flashat-app",
  "version": "1.0.0",
  "description": "Flashat Instant Messaging App",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon server/index.js",
    "client": "cd client && npm run dev",
    "deploy": "npm install --legacy-peer-deps --prefix server && ls -la server/ && cd client/ && install vite && ls -la node_modules/ && npm install --legacy-peer-deps --prefix client && ls -la client/ && npm run build --prefix client"
  },
  "keywords": [
    "flashat",
    "instant messaging",
    "app",
    "MERN + Socket.io"
  ],
  "author": "Azizi Hamza",
  "license": "ISC"
}
